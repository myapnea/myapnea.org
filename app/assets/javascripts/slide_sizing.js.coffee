@slidesReady = () ->
  # winH = 700 # $(window).height()
  # navH = $('nav').height()
  # # $('.row-fullscreen').height(winH - navH)
  # $('#slide-1').css('margin-top', navH)
  # $('#slide-1').height(800);

  # # Landing page - community highlights
  # highlightH1 = $('#community-highlight-1').height();
  # highlightH2 = $('#community-highlight-2').height();
  # highlightH3 = $('#community-highlight-3').height();

  # finalHighlightH = Math.max(highlightH1,highlightH2,highlightH3)
  # console.log(finalHighlightH)

  # $('.community-highlight').height(finalHighlightH)
