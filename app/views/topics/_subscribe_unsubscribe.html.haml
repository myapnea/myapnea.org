- case topic.subscription_type(current_user) when 'subscribed'
  You are subscribed to this topic. You may
  = link_to '<span class="glyphicon glyphicon-volume-off"></span> unsubscribe'.html_safe, subscription_forum_topic_path(topic.forum, topic, notify: '0'), method: :post, class: 'btn btn-xs btn-danger'
  to stop receiving updates for this topic.
- when 'muted', 'auto-muted'
  You may
  = link_to '<span class="glyphicon glyphicon-envelope"></span> subscribe'.html_safe, subscription_forum_topic_path(topic.forum, topic, notify: '1'), method: :post, class: 'btn btn-xs btn-primary'
  to receive updates for this topic.
- else  # 'auto-subscribed'
  You are auto-subscribed to this topic. You may
  = link_to '<span class="glyphicon glyphicon-volume-off"></span> unsubscribe'.html_safe, subscription_forum_topic_path(topic.forum, topic, notify: '0'), method: :post, class: 'btn btn-xs btn-danger'
  to stop receiving updates for this topic or change your
  = link_to '<span class="glyphicon glyphicon-cog"></span> settings'.html_safe, settings_path, class: 'btn btn-xs btn-default'
  to disable auto-subscribing for all forum topics.
