= form_with model: [:slice, subject], local: true do |form|
  = render "forms/horizontal/text_field", form: form, object: subject, key: :project_id
  = render "forms/horizontal/submit", form: form, object: [:slice, subject]
