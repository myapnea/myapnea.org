.form-group.string-container
  .input-group
    = render "slice/survey/decoration/addon", addon: @variable.prepend
    = form.text_field "response", class: "form-control #{"is-invalid" if @json.dig("errors", @variable.name).present?}", value: response, autocomplete: "off"
    = render "slice/survey/decoration/addon", addon: @variable.append
  = render "slice/survey/decoration/errors"
  = render "slice/survey/decoration/field_note"
