= form_for current_user, url: set_user_type_path do |f|
  .survey-container.check-w-letter
    .panel
      .question-container
        %h3.f300 In the realm of sleep apnea, how do you best describe yourself?
        %h5.f300.reset-layout (Check all that apply)
      .input-container
        - input_name = 'race'
        .check-box-container
          = f.check_box :provider, data: { hotkey: 'A' }
          = f.label :provider do
            %span A
            %p Provider
        .check-box-container
          = f.check_box :researcher, data: { hotkey: 'B' }
          = f.label :researcher do
            %span B
            %p Researcher
        .check-box-container
          = f.check_box :adult_diagnosed, data: { hotkey: 'C' }
          = f.label :adult_diagnosed do
            %span C
            %p Adult who has been diagnosed with sleep apnea
        .check-box-container
          = f.check_box :adult_at_risk, data: { hotkey: 'D' }
          = f.label :adult_at_risk do
            %span D
            %p Adult who is at-risk of sleep apnea
        .check-box-container
          = f.check_box :caregiver_adult, data: { hotkey: 'E' }
          = f.label :caregiver_adult do
            %span E
            %p Caregiver of adult diagnosed with or at-risk of sleep apnea
        .check-box-container
          = f.check_box :caregiver_child, data: { hotkey: 'F' }
          = f.label :caregiver_child do
            %span F
            %p Caregiver of child(ren) diagnosed with or at-risk of sleep apnea
      .button-container
        = f.submit "Submit", class: 'btn btn-primary btn-lg'
