- @title = "Edit User"

- @body = "sky"
- @footer = "grass"

- content_for :header do
  .header-container
    .container
      %h1.page-heading
        = link_to @user.name_was, @user
        Edit

.drawer-and-shelf-container
  .drawer
    = render "admin/menu"
  .shelf
    .dashboard-container
      = render "form", user: @user
