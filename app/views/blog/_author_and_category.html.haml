.broadcast-info
  .float-right
    &nbsp;
    = link_to blog_post_path(broadcast.url_hash.merge(anchor: 'comments')), class: 'no-underline', data: { turbolinks: false } do
      %i.fa.fa-comments
      = broadcast.replies.count
  By
  = link_to broadcast.user.forum_name, blog_author_path(author: broadcast.user.forum_name)
  on
  %i.hidden-xs= broadcast.publish_date.strftime '%B %-d, %Y'
  %i.hidden-sm.hidden-md.hidden-lg= broadcast.publish_date.strftime '%b %-d, %Y'
  - if broadcast.category
    in
    = link_to broadcast.category.name, blog_category_path(category: broadcast.category)
