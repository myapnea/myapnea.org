- if @survey
  .container
    .content-container.inherit-background
      = render 'surveys/partials/show'
- else
  .container
    .content-container
      - if current_user.provider?
        = render 'providers/providers_profile', part_of_registration: true
      - else
        = render 'social/social_profile'
