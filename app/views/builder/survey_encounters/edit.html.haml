.content-main-header
  %h1
    = link_to 'Surveys', builder_surveys_path
    &middot;
    = link_to @survey.name, builder_survey_path(@survey)
    &middot;
    - if @survey_encounter.encounter
      = link_to @survey_encounter.encounter.name, builder_survey_survey_encounter_path(@survey, @survey_encounter)
      &middot;
    Edit

.content-container
  .content-body
    = render 'builder/survey_encounters/form'
