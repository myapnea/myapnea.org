.content-main-header
  %h1
    = link_to 'Surveys', builder_surveys_path
    &middot;
    = link_to @survey.name, builder_survey_path(@survey)
    &middot;
    = link_to @question.slug, builder_survey_path(@question)
    &middot;
    = link_to @answer_template.name_was, builder_survey_question_path(@survey, @question, @answer_template)
    &middot;
    Edit

.content-container
  .content-body
    = render 'builder/answer_templates/form'
