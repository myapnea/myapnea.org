$("#progress").html("<%=j render 'admin/exports/progress' %>");

<% if @admin_export.total_steps > 0 && @admin_export.total_steps == @admin_export.current_step %>
clearInterval(<%= params[:interval] %>);
<% end %>
