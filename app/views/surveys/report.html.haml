= render partial: 'surveys/report_intro'

= render partial: "surveys/reports/#{@survey.slug.underscore}" #rescue render partial: 'surveys/reports/default'

.panel{ style: 'border: 2px solid #b1d2f6' }
  .panel-body
    %p.lead.no-margin
      %span.f500 Note:
      You can view the full-detailed version of the survey report by clicking
      = link_to 'here.', report_detail_survey_path

= render partial: 'surveys/report_outro'
