/ Must be provided a question object: question
= form_tag(process_answer_path, {role: "form"}) do
  = hidden_field_tag "question_id[]", question.id
  .panel.survey-container.progress-w-enter.progress-w-letter
    .question-container
      %h3.f300= question.text
    .input-container
      - answer_template = question.answer_templates.first
      - answer_template.answer_options.distinct.each do |answer_option|
        = radio_button_tag "#{question.id}[#{answer_template.id}]", answer_option.id, nil, id: "question_#{question.id}_#{answer_template.id}_#{answer_option.new_value}"
        %label.survey-multiple-input-button{ for: "question_#{question.id}_#{answer_template.id}_#{answer_option.new_value}" }
          %p= answer_option.text
          %span= answer_option.hotkey
