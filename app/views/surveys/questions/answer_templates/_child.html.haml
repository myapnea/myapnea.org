- if child_template.data_type == 'text_value'
  %div
    = text_field_tag "#{question.id}[#{child_template.id}]", answer.value[child_template.id], name: "response[#{child_template.id}]", class: "survey-short-text survey-text-small", disabled: answer.locked?, data: { secondary: true }
- elsif child_template.data_type == 'answer_option_id'
  - child_template.answer_options.each do |answer_option|
    %div{ class: 'label label-default label-sm' }
      = radio_button_tag "#{question.id}[#{child_template.id}]", answer_option.id, have_checked?(answer, child_template, answer_option.id), name: "response[#{child_template.id}]", id: "question_#{question.id}_#{child_template.id}_#{answer_option.new_value}", data: { hotkey: "#{answer_option.hotkey unless answer.locked?}", secondary: true }, disabled: answer.locked?
      %label{ for: "question_#{question.id}_#{child_template.id}_#{answer_option.new_value}", style: "#{'cursor: default' if answer.locked?}" }
        %p= answer_option.text
        %span.hotkey= answer_option.hotkey
  %br{ style: 'clear: both' }
