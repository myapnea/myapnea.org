%p{ style: p_style }
  Dear
  = succeed ',' do
    = @resource.first_name

%p{ style: p_style }
  You can confirm your account email through the link below:

%p{ style: center_style }
  = link_to 'Confirm my account', "#{ENV['website_url']}/confirmation?confirmation_token=#{@token}", style: link_style
